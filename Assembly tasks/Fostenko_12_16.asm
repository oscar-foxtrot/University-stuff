ZERO PROC
	PUSH EBP
	MOV EBP, ESP
	PUSH EDI
	PUSH ECX
	
	MOV EDI, [EBP+8]
	MOV ECX, [EBP+12]
	XOR EAX, EAX
	CLD
	REPNE SCASD
	JE opt2
	MOV AL, 0
	JMP finz
opt2: MOV AL, -1

finz: POP ECX
	POP EDI
	POP EBP
	RET 8
ZERO ENDP